info:
  name: Update
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{baseUrl}}/api/trips/00000000-0000-0000-0000-000000000000"
  body:
    type: json
    data: |-
      {
        "name": "Wonderland Trail",
        "status": "in_progress"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
