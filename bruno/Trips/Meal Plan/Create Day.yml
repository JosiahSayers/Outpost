info:
  name: Create Day
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/api/trips/00000000-0000-0000-0000-000000000000/meal-plan/days"
  body:
    type: json
    data: |-
      {
        "dayNumber": 1,
        "date": "2026-08-10"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
