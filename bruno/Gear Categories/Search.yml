info:
  name: Search
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/gear-categories?query=shelter"
  params:
    - name: query
      value: shelter
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
