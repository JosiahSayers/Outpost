info:
  name: Update
  type: http
  seq: 3

http:
  method: PUT
  url: "{{baseUrl}}/api/gear-inventory/1"
  body:
    type: json
    data: |-
      {
        "name": "Durston X-Mid 1",
        "quantity": 1,
      //   "existingCategoryId": 1,
        "newCategoryName": "Shelter"
      //   "grams": undefined
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
