info:
  name: Delete
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{baseUrl}}/api/gear-inventory/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
