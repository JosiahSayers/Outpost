info:
  name: Settings
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/account/settings"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
