info:
  name: Established Metadata
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/admin/meals/established-metadata"
  auth: inherit

docs: |
  Distinct sourceVendor/brand values across the whole public meal catalog,
  used to populate the vendor/brand filter options in the admin search UI.

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
