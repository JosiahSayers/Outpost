info:
  name: Delete
  type: http
  seq: 2

http:
  method: DELETE
  url: "{{baseUrl}}/api/packing-lists/10/sections/55"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
