info:
  name: Edit
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/api/packing-lists/10/sections"
  body:
    type: json
    data: |-
      {
        "name": "Clothing",
        "sortPosition": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
