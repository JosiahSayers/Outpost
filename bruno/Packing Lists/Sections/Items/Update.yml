info:
  name: Update
  type: http
  seq: 2

http:
  method: PATCH
  url: "{{baseUrl}}/api/packing-lists/10/sections/55/items/1"
  body:
    type: json
    data: |-
      {
        "name": "Tent stakes",
        "quantity": 8,
        "optional": false
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
