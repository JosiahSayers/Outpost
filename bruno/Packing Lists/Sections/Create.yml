info:
  name: Create
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/api/packing-lists/10/sections"
  body:
    type: json
    data: |-
      {
        "name": "Shelter",
        "sortPosition": 0
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
