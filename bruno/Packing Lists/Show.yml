info:
  name: Show
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/packing-lists/5"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
