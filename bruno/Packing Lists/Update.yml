info:
  name: Update
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{baseUrl}}/api/packing-lists/10"
  body:
    type: json
    data: |-
      {
        "name": "Josiah's Packing List",
        "description": "Three-season backpacking kit"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
