info:
  name: Update
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/api/packing-lists/10"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
