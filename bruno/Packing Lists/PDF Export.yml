info:
  name: PDF Export
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/api/packing-lists/4/pdf"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
