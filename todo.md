# IP Lookup Tasks

- Kick off download job when app starts
- Wire lookup into session API endpoint
- Wire lookup result into session transformer
- Surface lookup result in session UI
- Add attribution to admin footer `<a href='https://db-ip.com'>IP Geolocation by DB-IP</a>`
